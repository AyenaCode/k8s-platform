*.local.*
.env
*.log

# Frontend (Tier 1)
frontend/node_modules/
frontend/dist/

# Backend (Tier 2)
backend/bin/
*.test

# CLI (klab) — built binaries, never committed (the release workflow builds them)
cli/dist/
cli/klab
cli/klab.exe

# OS / editor
.DS_Store
